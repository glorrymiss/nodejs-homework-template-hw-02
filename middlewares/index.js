const isAuthenticate = require("./isAuthenticate");
const isValidId = require("./isValidId");
const validateBody = require("./validateBody")
const upload = require("./isAvatar");
module.exports = {
  isAuthenticate,
  isValidId,
  validateBody,
  upload,
};
